
ctrl-b
:swap-window -s 2 -t 1


s is source and t is target.

:swap-window -t 1 

(Omitting -s defaults to the current window)

Add `set -g renumber-windows on` to your config to remove gaps in your window numbers.
