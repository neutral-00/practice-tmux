# tmux-yank

Install the below tmux plugin to yank the same way as yanking in vim.

Include the below line in `.tmux.conf`

```
set -g @plugin 'tmux-plugins/tmux-yank'
```
Enter copy mode with `<prefix>[`

